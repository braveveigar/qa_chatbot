## Commit Message Convention

We follow Conventional Commits:

type(scope): subject

Examples:
- feat(auth): add JWT login
- fix(redis): initialize redis client at module load
- refactor(chat): separate service layer

Types:
feat, fix, refactor, chore, docs, test